// relative imports
// you can import a class from another file
// this way, you can write the realtive path of the file
// like "./construtores.dart" or... because it is on the same folder

import '02 construtores.dart';

main() {
  print(Data(30, 06).getDate());
}